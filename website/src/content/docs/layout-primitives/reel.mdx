import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';
import { Tabs } from 'nextra/components';
import { PreviewConainer, Badge, DummyImage } from '../../../components';

# Reel

Displays child elements in a simple horizontal scroll.

## Usage

<PreviewConainer>
	<Reel>
		<DummyImage />
		<DummyImage />
		<DummyImage />
	</Reel>
</PreviewConainer>

<Tabs items={['HTML', 'JSX']}>
	<Tabs.Tab>
		```html
		<div data-unitone-layout="reel">
			<img src="..." alt="" />
			<img src="..." alt="" />
			<img src="..." alt="" />
		</div>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		```jsx
		import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';

		<Reel>
			<img src="..." alt="" />
			<img src="..." alt="" />
			<img src="..." alt="" />
		</Reel>
		```
	</Tabs.Tab>
</Tabs>

## Props

### <Badge>HTML</Badge> -no-bar / <Badge>JSX</Badge> noBar

Hide bar.

<Tabs items={['HTML', 'JSX', 'Preview']}>
	<Tabs.Tab>
		```html
		<div data-unitone-layout="reel -no-bar">
			...
		</div>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		```jsx
		import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';

		<Reel noBar>
			...
		</Reel>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		<PreviewConainer>
			<Reel noBar>
				<DummyImage />
				<DummyImage />
				<DummyImage />
			</Reel>
		</PreviewConainer>
	</Tabs.Tab>
</Tabs>

### <Badge>HTML</Badge> -gap:\{variation\} / <Badge>JSX</Badge> gap=\{variation\}

Gap.

Can be set from `-3` to `7`. For variations `2` and above, `clamp` is used to adjust the margins so that they do not become too large on small screens.
If it is `1` or more, adding `s` will shrink it to size `-1` on mobile. (e.g. `1s`)
If it is `2` or more, adding `m` will shrink it to size `1` on mobile. (e.g. `2m`)

<Tabs items={['HTML', 'JSX', 'Preview']}>
	<Tabs.Tab>
		```html
		<div data-unitone-layout="reel -gap:-2">
			...
		</div>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		```jsx
		import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';

		<Reel gap={-2}>
			...
		</Reel>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		<PreviewConainer>
			<Reel gap={-2}>
				<DummyImage />
				<DummyImage />
				<DummyImage />
			</Reel>
		</PreviewConainer>
	</Tabs.Tab>
</Tabs>

### <Badge>HTML</Badge> --unitone--height / <Badge>JSX</Badge> height=\{value\}

Height.

<Tabs items={['HTML', 'JSX', 'Preview']}>
	<Tabs.Tab>
		```html
		<div data-unitone-layout="reel" style="--unitone--height: 250px">
			...
		</div>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		```jsx
		import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';

		<Reel height="250px">
			...
		</Reel>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		<PreviewConainer>
			<Reel height="250px">
				<DummyImage />
				<DummyImage />
				<DummyImage />
			</Reel>
		</PreviewConainer>
	</Tabs.Tab>
</Tabs>

### <Badge>HTML</Badge> --unitone--item-width / <Badge>JSX</Badge> itemWidth=\{value\}

`width` of the items.

<Tabs items={['HTML', 'JSX', 'Preview']}>
	<Tabs.Tab>
		```html
		<div data-unitone-layout="reel" style="--unitone--item-width: 250px">
			...
		</div>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		```jsx
		import { Reel } from '@inc2734/unitone-css/layout-primitives/reel/react';

		<Reel itemWidth="250px">
			...
		</Reel>
		```
	</Tabs.Tab>
	<Tabs.Tab>
		<PreviewConainer>
			<Reel itemWidth="250px">
				<DummyImage />
				<DummyImage />
				<DummyImage />
			</Reel>
		</PreviewConainer>
	</Tabs.Tab>
</Tabs>
